import React from 'react';
import { Route, Routes } from 'react-router';

export default (
    <Routes>
        <Route path='/' />
        <Route path='/technology' />
        <Route path='/service' />
        <Route path='/company' />
        <Route path='/recruit' />
    </Routes>
);
